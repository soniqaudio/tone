"use client";

export function MixerView() {
  return (
    <div className="flex h-full w-full flex-col items-center justify-center gap-3 bg-zinc-950/60 text-zinc-500">
      <div className="text-sm font-semibold uppercase tracking-[0.3em] text-zinc-600">Mixer</div>
      <p className="text-xs text-zinc-500">
        Mixer interface coming soon. Use the view switcher to jump back to the piano roll.
      </p>
    </div>
  );
}
